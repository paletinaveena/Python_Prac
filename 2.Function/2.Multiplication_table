# Create a fun that prints a table--printTable(num,length) default length is 20
def printTable(num, length = 20):
    for i in range(1, length+1):
        print(f"{num} * {i} = {num*i}")

printTable(5, 5)
