# Write a program which can map() to make a list whose elements are square of numbers between 1 and 20 (both included).

# Method - 1
rang = int(input("Enter range:"))
l = []
for num in range(1, rang+1):
  l.append(num)
sq_l = list(map(lambda x : x**2, l))
print(sq_l)

# Method - 2
rang = int(input("Enter range:"))
sq_l = list(map(lambda x : x**2, range(1,rang+1)))
print(sq_l)
