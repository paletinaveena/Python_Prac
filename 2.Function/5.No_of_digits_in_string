# Create a function that takes a string and returns number of digits in the string
def count_digits(st):
    count = 0
    for c in st:
        if c.isdigit():
            count += 1
    return count

print(count_digits("Abc123xyz"))
